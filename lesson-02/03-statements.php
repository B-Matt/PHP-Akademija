<?php
header('Content-Type: text/plain');

/*
    //$isTrue = true;
    $isTrue = ':]';

    if($isTrue) {
        echo "It's true\n";
    }

    $isFalse = false;
    if($isFalse) {
        echo "It's false";
    }

    if($isTrue || $isFalse)
        echo "It's true or false"; 

    var_dump(123 == '123abc');
    var_dump((1 == 1.0) == (true == 'false'));
    var_dump(1 === '1');

    $a = 5;
    $b = 6;

    if($a > $b) {
        echo 'a bigger than b';
    }
    else if($a == $b) {
        echo 'a is equal b';
    }
    else {
        echo 'a is smaller than b';
    }

    $isTrue = false;
    $result = $isTrue ? 'one' : 'two';
    var_dump($result);

    // is_array(), is_number(), is_int()

    if(isset($lol) && $lol == 3) {

    }
    // isset(), empty()
 */